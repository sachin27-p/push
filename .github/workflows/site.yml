name: Build PUSH to GCR

on:
  push:
    branches:
    - 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest
    env: 
        image_name: webpage
    steps:

    - name: code checkout
      uses: actions/checkout@v4

    - name: install the gcloud 
      uses: google-github-actions/setup-gcloud@c0590608abfef96ce4f0e42d1dd61a3f3de158ea3afb81fbb1c37f0772a9d5a2
      with:
        project_id: ${{ secrets.GCP_PROJECT }}
        service_account_key: ${{ secrets.GCP_SA_KEY }}
        #install_components: 'gke-gcloud-auth-plugin'
        export_default_credentials: true

    - name: build and push the docker image
      env:
        GCP_PROJECT: ${{ secrets.GCP_PROJECT }}
      run: |
        gcloud auth configure-docker us-central1-docker.pkg.dev
        docker build -t us-central1-docker.pkg.dev/kube-429706/admin/webpage:latest .
        docker push us-central1-docker.pkg.dev/kube-429706/admin/webpage:latest
    #- name: deploy to gke
     # env:
      #    GCP_PROJECT: ${{ secrets.GCP_PROJECT }}
      #run: |
       #   gcloud container clusters get-credentials autopilot-cluster-1 --region us-central1
        #  sed -i "s/GCP_PROJECT/$GCP_PROJECT/g" web.yml
         # kubectl apply -f web.yml